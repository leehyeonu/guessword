rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Firestore는 백엔드 Firebase Admin SDK에서만 접근합니다.
    // 브라우저/외부 클라이언트 SDK 요청은 모든 컬렉션에서 차단합니다.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
